const express = require("express"); // Імпорт Express для створення API
const router = express.Router(); // Створення роутера для користувачів, який буде використовуватися для визначення маршрутів, пов'язаних з операціями над користувачами, такими як отримання, створення, оновлення та видалення користувачів

const controller = require("../controllers/usersController"); // Імпорт контролера для користувачів, який містить логіку обробки запитів, пов'язаних з користувачами, таких як отримання всіх користувачів, отримання користувача за ID, створення нового користувача, оновлення існуючого користувача та видалення користувача

router.get("/", controller.getAll); // Маршрут для отримання всіх користувачів, який викликає метод getAll з контролера користувачів і повертає результат у форматі JSON з статусом 200
router.get("/:id", controller.getById); // Маршрут для отримання користувача за ID, який викликає метод getById з контролера користувачів, передаючи ID користувача з параметрів запиту, і повертає результат у форматі JSON з статусом 200, або помилку, якщо користувача не знайдено
router.post("/", controller.create); // Маршрут для створення нового користувача, який викликає метод create з контролера користувачів, передаючи дані нового користувача з тіла запиту, і повертає створеного користувача у форматі JSON з статусом 201, або помилку, якщо дані користувача не валідні
router.put("/:id", controller.update); // Маршрут для оновлення існуючого користувача за ID, який викликає метод update з контролера користувачів, передаючи ID користувача з параметрів запиту та нові дані користувача з тіла запиту, і повертає оновленого користувача у форматі JSON з статусом 200, або помилку, якщо користувача не знайдено або дані не валідні
router.delete("/:id", controller.remove); // Маршрут для видалення користувача за ID, який викликає метод remove з контролера користувачів, передаючи ID користувача з параметрів запиту, і повертає статус 204, якщо видалення було успішним, або помилку, якщо користувача не знайдено

module.exports = router; // Експорт роутера для користувачів, який буде використовуватися в інших частинах програми, таких як файл index.js, де він буде підключений до основного додатку Express для обробки запитів, пов'язаних з користувачами